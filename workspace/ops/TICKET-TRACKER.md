- [SYSTEMIC] **Exec gate chronic blockage** (343h+ exec probes blocked across cycles) causing agent action delays and preventing P0 ticket resolution. Requires clearing of approval wall or config adjustment to restore exec functionality. **Priority:** P0 systemic. **Owner:** OPS/ANURAG. **Target resolution:** within 24h.

- [NEW CYCLE-17 RED SELF-IMPROVEMENT 2026-06-25 12:21 EDT] **Agent-status observability gap — self-heal placeholders indistinguishable from real worker writes** (RED cycle 17). All 6 specialist agent-status files (eng/research/finance/ops/infosec/allrounder) read at 12:21 EDT show identical pattern: `source: agent-status-refresh.sh (self-heal writer — real agent will overwrite on next check-in)`, `proactiveWork: Awaiting agent's own status write on next refuel tick`, `workerHeartbeat: alive`. This means the "healthy" status is a self-heal default, NOT evidence of actual agent work. The system cannot distinguish a productive idle agent from a dead one. Root cause: agent-status-refresh cron runs but specialist agents don't write through on their own cadence. **Priority:** P1 observability. **Owner:** OPS (scrutinize self-heal refresh vs real writes; add write-source attribution to status schema) + ENG (audit specialist agent refuel tick — are they firing?). **Proposed fix:** add `last_real_write` timestamp (separate from `lastCheck`) so RED/OPS can detect agents that haven't self-written in >N cycles. **Cross-ref:** RED Self-Improvement cycle 17 + LEARNINGS.md cycle 17 entry.

- [CYCLE-17 DERIVED] **Routing-decisions.jsonl + errors.jsonl read paths blocked by exec gate** — RED Self-Improvement cycle 17 cannot tail `~/.openclaw/workspace/logs/errors.jsonl` (3 consecutive `/approve` prompts id 62475df0, 2820dbc4, plus id 0f20da71 / 896b7d62 for routing-decisions) because exec is GATED chronic ~347h+. Self-improvement cycle must work around exec gate by using read tool only. **Priority:** P3 derived. **Owner:** OPS (exec gate chronic). **Note:** reflects deeper exec gate issue, not a separate defect.