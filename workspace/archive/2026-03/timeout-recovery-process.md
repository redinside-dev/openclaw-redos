When `workspace/logs/dispatch.jsonl` shows a task stuck in `PENDING` after a dispatch timeout, confirm whether an equivalent task was already completed (for AUTO-030, AUTO-028/AUTO-021 coordination protocol work superseded it) before retrying to avoid duplicate execution. If superseded, mark the task obsolete in queue tracking and append a recovery note to `tasks-log.md`; if not superseded, re-dispatch with a fresh runId and monitor for completion within the next cycle.
