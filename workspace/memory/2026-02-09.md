# 2026-02-09

## OpenClaw CLI: separate Codex OAuth per agent (ZEN vs RED)
- User goal: RED already logged into Codex with one account; wants ZEN to use a separate account.
- Observed: `openclaw agent auth login --agent allrounder` fails on OpenClaw 2026.2.9; auth moved under `openclaw models auth ...`.
- Correct approach (current CLI): run auth flow with per-agent override:
  - `openclaw models --agent allrounder auth login` (interactive provider/method selection)
  - Then verify: `cat ~/.openclaw/agents/allrounder/agent/auth-profiles.json`
  - RED remains separate under: `~/.openclaw/agents/main/agent/auth-profiles.json`
- Pitfall: user ran `please-fix` and got CURSOR_API_KEY invalid; this is unrelated to Codex OAuth.
- Tip: use incognito/different browser profile to avoid OAuth reusing the wrong Google session.
