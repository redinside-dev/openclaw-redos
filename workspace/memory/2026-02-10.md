# 2026-02-10

## Ollama / MiniCPM enablement

- Installed local Ollama models:
  - `openbmb/minicpm-o2.6:8b`
  - `openbmb/minicpm-v4.5:latest`
- Verified via Ollama HTTP API (`/api/generate`) that:
  - `minicpm-o2.6:8b` can return clean JSON in a simple JSON-only prompt.
  - `minicpm-v4.5:latest` prepends `<think>` (violates JSON-only) and is slower; do not use as primary for structured outputs.

## OpenClaw integration + routing changes

- Enabled Ollama provider in OpenClaw so `ollama/<model>` ids are recognized:
  - Set `env.vars.OLLAMA_API_KEY = "ollama-local"`
  - Added explicit `models.providers.ollama` with baseUrl `http://127.0.0.1:11434/v1` and model entries.
  - Confirmed `openclaw models list --plain` shows `ollama/openbmb/minicpm-o2.6:8b`.

- At Anurag’s request, set ENG/OPS/FINANCE/RESEARCH primary model to local MiniCPM:
  - primary: `ollama/openbmb/minicpm-o2.6:8b`
  - fallback: `zai/glm-4.7`
  - RED+ZEN remain `openai-codex/gpt-5.2`.

## Token burn mitigation / manual-only

- Due to heavy token burn and quota exhaustion, cron jobs were disabled (manual-only). Cron was later found re-enabled; manual-only was restored again by disabling all cron jobs.

## Security hardening for small local models

- OpenClaw security audit flagged CRITICAL: small models require sandboxing + web tools disabled.
- Applied mitigation:
  - `agents.defaults.sandbox.mode = "non-main"`
  - For agents {eng, ops, finance, research}: `tools.deny = ["group:web", "browser"]`
- After mitigation, audit still reports CRITICAL but now confirms web tools are off; remaining recommendation is sandbox mode `all` (not applied yet; requires explicit approval).

## Telegram outage fix

- Telegram bot non-responsiveness traced to telegram plugin accidentally disabled in `plugins.entries.telegram.enabled=false`; re-enabled and restarted gateway; Telegram returned OK (6/6).

## Employee-agent reliability issues (locks / timeouts)

- Observed repeated failures for employee agents (ENG/OPS/RESEARCH/FINANCE):
  - `session file locked (timeout 10000ms): ... .jsonl.lock` (often after SIGKILL / aborted runs)
  - gateway WS timeouts (90–150s) followed by embedded fallback.
- Cleared at least one stale lock by renaming `*.jsonl.lock` to `*.stale.<timestamp>`.

## Model id / context fixes

- Found mismatch between configured Ollama model ids and installed Ollama tags.
- Updated employee agents to prefer the actual installed 16k tag:
  - `ollama/minicpm-o2.6-16k:latest` (ctxWindow=16384) rather than `ollama/openbmb/minicpm-o2.6:8b` (configured as 8192).
- Added `ollama/minicpm-o2.6-16k:latest` to `agents.defaults.models`.

## Tool-policy fixes for Ollama

- Ollama endpoint returned: `400 ... does not support tools` when OpenClaw included tool schemas.
- Applied global restriction: `tools.byProvider.ollama.deny = ["*"]` to prevent sending any tools/tool schemas to Ollama-backed runs.

## Concurrency experiment

- Temporarily increased `agents.defaults.maxConcurrent` to 3 to reduce queueing, but it increased lock contention; reverted back to 1.

## ENG agent (Option B) - no fallback + alternative local model

- At Anurag’s request, switched ENG to **Option B**: **no fallback** and use a more instruction-following local Ollama model.
- Set ENG primary to `ollama/llama3.1:8b`.
- Identified OpenClaw hard minimum ctx of **16000** tokens; initial `llama3.1:8b` entry was misdeclared as 8192 and got blocked.
- Fixed the configured ctx for `ollama/llama3.1:8b` to **131072** (per `ollama show llama3.1:8b`), enabling it to pass OpenClaw’s ctx minimum.

## ENG fallback request (pending)

- New request: ENG primary `ollama/llama3.1:8b`, ENG secondary `ollama/qwen2.5-coder:*`.
- `qwen2.5-coder` was not installed; started `ollama pull qwen2.5-coder:7b` (~4.7GB) to satisfy the `*` fallback choice.
