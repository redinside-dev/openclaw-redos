# 2026-02-07

- Cursor CLI entrypoint to use is `cursor-agent` (installed at `~/.local/bin/cursor-agent`). `cursor-agent status` confirms logged in as `anuragsaxena.ai@gmail.com`. Do not rely on `CURSOR_API_KEY` env var; prefer login-based auth.
- Windsurf installed and CLI enabled via symlink: `/opt/homebrew/bin/windsurf` → `/Applications/Windsurf.app/Contents/Resources/app/bin/windsurf` (available, but **do not use for coding** unless explicitly requested).
- Created shared OpenClaw skill `skills/x-mirror` to read X/Twitter posts without login by rewriting `https://x.com/...` → `https://r.jina.ai/https://x.com/...`.
- Updated coding policy (per Anurag): **Cursor Agent CLI only** for coding tasks; default coding model is **Claude Sonnet 4.5** (`cursor-agent --model sonnet-4.5`).
- Verified Cursor Agent + Java toolchain setup: `cursor-agent login/status/models` working; Java available. Used `cursor-agent --model sonnet-4.5` to generate `/Users/redinside/Development/Codebase/projects/Test/Adder.java`, compiled with `javac`, ran `java Adder "2+2+2+2"` → output `8`.
- Model routing split finalized and verified:
  - **Red Inside (main agent)**:
    - Agent ID: `main`
    - Telegram bot: @RedinsideBot (accountId=default)
    - Primary model: `openai-codex/gpt-5.2`
    - Fallback model: `moonshot/kimi-k2.5`
    - Role: Principal Software Architect + Elite Java Engineer + Chief Investment Strategist
    - Purpose: Technical work, investments, deep reasoning
  - **Zen (allrounder agent)**:
    - Agent ID: `allrounder`
    - Telegram bot: @ZenRedBot (accountId=allrounder)
    - Primary model: `zai/glm-4.7`
    - Fallback model: `moonshot/kimi-k2.5`
    - Role: Daily-driver assistant
    - Purpose: Fast/cheap general Q&A, summaries, operational help, lightweight research
  - ZAI_API_KEY stored in OpenClaw config env vars.
  - OpenClaw bindings configured correctly for both bots.
- Perplexity migration planned for Zen (allrounder):
  - **Goal:** Set Perplexity as primary model for Zen (replace GLM)
  - **New config:** Primary `perplexity/sonar-medium-online` → fallback GLM → Kimi
  - **Backup files created:**
    - `models.json.backup-20260207-205810`
    - `openclaw.json.backup-20260207-205813`
  - **Rollback plan documented:** `workspace/rollback-plan.md`
  - **Successfully migrated to Perplexity:** Zen now uses `perplexity/sonar` as primary model (fastest Perplexity for basic chat).
  - Fallback chain: Sonar → Sonar Medium Online → GLM → Kimi
  - Perplexity API configured in `models.json` (OpenAI-compatible provider)
  - **Web search tool configured:** `web_search` now uses Perplexity as primary provider (`perplexity/sonar` model)

- **CANONICAL CONFIGURATION LOCKED (2026-02-07 23:45 EST):**
  - **RED (main):** Telegram @RedinsideBot | Primary: `openai-codex/gpt-5.2` | Fallback: `moonshot/kimi-k2.5`
  - **ZEN (allrounder):** Telegram @ZenRedBot | Primary: `perplexity/sonar` | Fallbacks: `zai/glm-4.7` → `moonshot/kimi-k2.5`
  - **ZEN nickname:** "ZEN" (all caps)
  - **Coding:** `cursor-agent --model sonnet-4.5`
  - **POLICY:** Never change unless Anurag explicitly asks
  - **Fix applied:** added Perplexity provider token to both agent auth stores (`.../agents/main/.../auth-profiles.json` and `.../agents/allrounder/.../auth-profiles.json`) so ZEN can actually run `perplexity/sonar` (verified via agent-to-agent test response).
