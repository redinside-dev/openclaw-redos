# 2026-02-10

## OpenClaw upgrade + verification
- Upgraded OpenClaw from 2026.2.6-3 → 2026.2.9 (user-approved).
- Pre-upgrade backups created:
  - ~/.openclaw/openclaw.json.bak-preupdate-20260209-171223
  - ~/.openclaw/cron.bak-preupdate-20260209-171223.tgz
- Post-upgrade: gateway healthy; Telegram OK.

## Search configuration
- Perplexity web_search model id fixed from `perplexity/sonar` → `sonar` (restored web_search functionality).

## Claude Code
- Claude Code CLI present: /Users/redinside/.local/bin/claude
- Version: 2.1.38 (Claude Code)
- Verified working interactively in Terminal; screenshot showed Claude Pro and model set to Sonnet 4.5 (`claude-sonnet-4-5-20250929`) via `/model`.
- Non-interactive `claude -p ...` calls issued from OpenClaw exec hung and were SIGKILLed; likely due to non-TTY/non-interactive execution environment rather than Claude being broken.
- Plan: use Claude Code (Sonnet 4.5) as the engineering heavy-lifter/executor; keep GLM/Llama as planner/router; Perplexity remains web_search only.

## Incident: RedinsideBot non-response
- Error observed: "Agent failed before reply: All models failed" because both providers were in cooldown:
  - openai-codex/gpt-5.2: cooldown (rate_limit)
  - moonshot/kimi-k2.5: cooldown (rate_limit)
- Logs also show Moonshot billing/credits errors (insufficient balance) during cron runs.
- Mitigation options discussed: wait for cooldown, fix Moonshot billing, add a 3rd fallback provider (without changing Codex as primary).

## Incident: Telegram ↔ Gateway connection drop
- User reported temporary inability to connect from Telegram and TUI.
- Current status check showed Gateway service running and reachable locally (ws://127.0.0.1:18789) and Telegram probes OK for all bots.
- Likely root cause: gateway restart/reload during config/onboarding work causing temporary disconnect/reconnect.
- CLI note: `openclaw logs` uses `--limit` (not `--tail`).
